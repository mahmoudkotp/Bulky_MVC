@page
@model BulkyWebRazor_Temp.Pages.Categories.CreateModel

<form method="post">
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Create Category</h2>
            <hr />
        </div>
        @* أنواع asp-validation-summary
        يمكن أن يأخذ القيم التالية    :
        None → لا يعرض أي أخطاء.
        ModelOnly → يعرض فقط الأخطاء العامة (التي لم تُحدد لحقل معين).
        All → يعرض كل الأخطاء، سواء العامة أو المرتبطة بخصائص محددة في النموذج. *@
        @* <div asp-validation-summary="All"></div> *@

        <div class="row mb-3">
            <label  asp-for="Category.Name" class="p-1"></label>
            <input asp-for="Category.Name" type="text" class="form-control"/>
            <span asp-validation-for="Category.Name" class="text-danger"></span>
        </div
        <div class="row mb-3">
            <label asp-for="Category.DisplayOrder" class="p-1"></label>
            <input asp-for="Category.DisplayOrder" type="text" class="form-control"/>
            <span asp-validation-for="Category.DisplayOrder" class="text-danger"></span>
        </div>

        <div class="row">
            <div class="col-6 col-md-3">
                    <button type="submit" class="btn btn-primary form-control">Create</button>
            </div>
            <div class="col-6 col-md-3">
                    <a asp-page="/Categories/index" class="btn btn-outline-secondary form-control border">Back to List</a>
            </div>
        </div>
    </div>
</form>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}